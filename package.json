{
  "name": "mern-school-management-system",
  "version": "1.0.0",
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "scripts": {
    "dev:mac": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.0",
    "framer-motion": "^12.23.6",
    "jsonwebtoken": "^9.0.2",
    "react-icons": "^5.5.0",
    "styled-components": "^6.1.19"
  }
}

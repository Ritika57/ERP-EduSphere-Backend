{
  "name": "mern-school-management-system",
  "version": "1.0.0",
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2"
  }
}
